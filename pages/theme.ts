import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#13ed72;',
    },
    secondary: {
      main: '#b4edcd',
    },
  },
});

export default theme;
